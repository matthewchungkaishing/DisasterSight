[tool.ruff]
line-length = 100
target-version = "py311"
exclude = [
    ".git",
    ".ruff_cache",
    ".venv",
    "artifacts",
    "data/cache",
    "data/interim",
    "data/processed",
    "data/raw",
    "outputs",
]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
